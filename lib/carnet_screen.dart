import 'package:flutter/material.dart';

class CarnetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Carnet de usuario aquí'));
  }
}
